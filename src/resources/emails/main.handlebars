<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Title</title>
  <style>
    body {
      padding: 32px;
      background-color: #e0e0e0;
      font-family: Verdana, Geneva, Tahoma, sans-serif;
    }

    table {
      width: 100%;
    }

    .container {
      width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .content {
      margin-top: 24px;
      margin-bottom: 24px;
      padding: 24px;
      background-color: white;
      box-shadow: box-shadow: var(--tw-ring-offset-shadow, 0 0 #0000),
        var(--tw-ring-shadow, 0 0 #0000),
        0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
    }

    .text-center {
      text-align: center;
    }

    .text-xs {
      font-size: 12px;
    }

    .text-gray-500 {
      color: #a0a0a0;
    }
  </style>
</head>

<body>
  <div class="container">
    <header>
      {{> header}}
    </header>

    <div class="content">
      {{{body}}}
    </div>

    <footer>
      {{> footer}}
    </footer>
  </div>
</body>

</html>